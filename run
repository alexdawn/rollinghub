#!/usr/bin/env bash

set -e
set -u

CONTEXT=${1:-"default"}

export FLASK_APP=rollinghub
if [[ $CONTEXT == "dev" ]] ; then
    export FLASK_ENV=development
    flask init-db
    flask run
fi
if [[ $CONTEXT == "prod" ]] 0; then
    gunicorn rollinghub:rollinghub
fi