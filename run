#!/usr/bin/env bash

set -e
set -u

CONTEXT=${1:-"default"}

export FLASK_APP=rollinghub
if [[ $CONTEXT == "dev" ]] ; then
    export FLASK_ENV=development
fi
. activate rollinghub
flask init-db
flask run